import * as UI from '../actions/ui.js';

const init = {
  user_id: false,
  profile_picture: ''
}

const user = (state = init, action: any = {}) => {
  switch (action.type) {

    default:
      return state;
  }
};

export default user;
